apiVersion: account.btp.sap.crossplane.io/v1alpha1
kind: Subscription
metadata:
  namespace: default
  name: sub-test
spec:
  forProvider:
    appName: auditlog-viewer
    planName: free
  cloudManagementRef:
    name: e2e-sub-cis-local
